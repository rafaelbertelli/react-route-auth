/**
 * @isAuthenticated
 * Methods to verify auth must be defined into it.
 * Here, we are mocking 'true' or 'false' simulating back-end response
 * */
export const isAuthenticated = () => true;
// export const isAuthenticated = () => false;
